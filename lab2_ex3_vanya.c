#include <stdio.h>   
#include <math.h>    

int main() {

    // Оголошення змінних
    float x, y, f;

    // Виводимо формулу
    printf("Обчислення функції: f = (x^2 + y^2) / sqrt(x + y)\n\n");

    // Введення значення x
    printf("Введіть значення x: ");
    scanf("%f", &x);   

    // Введення значення y
    printf("Введіть значення y: ");
    scanf("%f", &y);

    // Обчислення формули
    // x * x = x^2
    // y * y = y^2
    // sqrt(x + y) = корінь
    f = (x * x + y * y) / sqrt(x + y);

    // Виведення результату
    printf("\n--- Результат ---\n");
    printf("Для x = %.2f та y = %.2f\n", x, y);
    printf("Значення функції f = %.4f\n", f);

    return 0; // завершення програми
}
