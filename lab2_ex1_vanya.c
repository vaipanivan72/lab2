#include <stdio.h>
#include <math.h> 

int main() {
    // Оголошуємо змінні дійсного типу 
    float x, y;
    float average, difference, product;

    // Введення даних з пояснювальними повідомленнями 
    printf("Введіть перше число (x): ");
    scanf("%f", &x);
    
    printf("Введіть друге число (y): ");
    scanf("%f", &y);

    // 1. Обчислення середнього арифметичного 
    average = (x + y) / 2.0;

    // 2. Обчислення різниці першого і другого числа 
    difference = x - y;

    // 3. Обчислення добутку другого числа і квадрату першого 
    product = y * pow(x, 2);

    // Виведення результатів обчислень 
    printf("\nРезультати роботи програми:\n");
    printf("Середнє арифметичне: %.2f\n", average);
    printf("Різниця 1-го і 2-го чисел: %.2f\n", difference);
    printf("Добуток 2-го і квадрату 1-го: %.2f\n", product);

    return 0;
}
